import sys
sys.stdin = open('≥Û¿€π∞_input.txt','r')


T = int(input())
size = int(input())
farms = []
for i in range(T):
    for _ in range(size)
        farm = list(map(int,input()))
        farms.append(farm)
        result = 0

        s = 0
        S_plus = size//2 + s
        S_minus = size//2 - s
        while S_size_p < size:
            for i in range(S_minus,S_minus):


mat = []
for i in range(1,size+1):
    mat.append(i)
    for j in mat:
